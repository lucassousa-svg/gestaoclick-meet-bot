{
  "name": "gestaoclick-meet-bot",
  "version": "1.0.0",
  "description": "Bot para entrar no Google Meet",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm solid-install || npm install && PUPPETEER_CACHE_DIR=bot-chrome npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.5.0",
    "ws": "^8.14.2"
  }
}
